exports.rand = function modify(min1,max1,min2,max2) {
	
var rand = Math.random() * (max - min) + min
var (users) = (users) + rand*(users)

var rand = Math.random() * (max - min) + min
var (rpu) = (rpu) + rand
};