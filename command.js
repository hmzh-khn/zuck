// Game state functions
var initGame = function(phoneNumber) {
	//register user as player in db
	//SET uid:phone_number:users
	//SET uid:phone_number:competitors
	//SET uid:phone_number:employees
	//SET uid:phone_number:features
	//SET uid:phone_number:investors
	//SET uid:phone_number:money
};
var ipo = function(phoneNumber) {
	//remove user from db
	
};

// User information
var help = function(phoneNumber) {
	//return help manual
	
};
var state = function(phoneNumber) {
	//return
};
var report = function(phoneNumber) {

};

//User interaction
var buy = function(company) {
	//lookup amount for company
	//remove amount from current balance
};

var devProject = function(phoneNumber) {
	//auto
};